const accountId = 45654
let userEmail = "trp@gmail.com"
var userName ="Tushar"
userState = "Rajkot"  // it allow without datatype , but it is possible
let userAddress;    // Undefined value

// accountId = 2 //  not allowed

console.log(accountId)

/*
    Prefer not to use var
    because of issue in block scope and functional scope
*/

userEmail = "brp@gmail.com"
userName = "Tushar"
userState = "Dwarka"

console.table([accountId, userEmail, userName, userState, userAddress])